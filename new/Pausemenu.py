import pygame

class Pausemenu:
    def __init__(self, prevgame, size):
        self.prevgame = prevgame

        self.screen = pygame.display.set_mode(size)
        


    def update(self):
        if(backbutton is pressed):
            return self.prevgame

    def draw(self):

        self.screen.fill((0, 0, 0))

        pygame.display.flip()